# Reference Only

Nothing under this directory is part of the active new library.

- `v1-library/`: original selectable library
- `source-guides/`: captured source guides and skills
- `prior-libv2-notes/`: earlier intake, proposals, and review notes

Copy useful material into `intake/` before review. Promote reviewed material
from `intake/` into `agents/`, `skills/`, or `guides/`.
